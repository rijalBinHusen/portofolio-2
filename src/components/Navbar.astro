---
   // import {  } from '@astrojs/vue'
   const navs = [
      { title: 'About', route: '/', icon: 'user'},
      { title: 'Blog', route: '/blog/', icon: 'newspaper'},
      { title: 'Github repo', route: '/projects/', icon: 'diagram-project'}
   ]

   

---
<script>
   let showMenu = false

   // // const route = useRoute()

   // const activePage = ref(null)

   // const isActive = (page: String) => {
   //    // console.log(route)
   //    return page === activePage.value
   // }

   let mobileView = false

   // const showPage = (page: String) => {
   //    activePage.value = page
   //    if(mobileView.value) {
   //       showMenu.value = false
   //    }
   // }

   const checkScreen = () => {
        if(window.innerWidth <= 750) {
            mobileView = true;
            showMenu = false
            return
        }
            mobileView = false;
            showMenu = true
      }
      // Detecting width device when the page first open
      checkScreen()
      // Detecting width device when browser resized
      window.addEventListener('resize', checkScreen)
   
</script>
<nav id="header" class="w-full z-30 top-10 shadow-lg border-b border-gray-300 py-1 rounded-lg">
     <div class="w-full flex items-center justify-between mt-0 px-6 py-2">
        <input class="hidden" type="checkbox" id="menu-toggle">
        <div class="md:flex md:items-center md:w-auto w-full order-3 md:order-1" id="menu">
             <!-- Navigation link -->
            <nav>
               <ol class="md:flex items-center justify-between text-base text-black pt-4 md:pt-0">
                     {navs.map((nav) => (
                     <li>
                        <a 
                           href={nav.route} 
                           class="inline-block no-underline hover:text-blue-600 font-medium text-lg py-2 px-4 lg:-ml-2"
                        >
                           {nav.title}
                        </a>      
                     </li>
                  ))}
               </ol>
            </nav>
            <!-- Navigation link -->
         </div>
        
        <div v-if="!showMenu" class="order-2 md:order-3 flex flex-wrap items-center justify-end mr-0 md:mr-4" id="nav-content">
           <div class="auth flex items-center w-full md:w-full">
              <a href="https://github.com/rijalBinHusen">
                 <!-- <font-awesome-icon class="text-xl" :icon="['fab', 'github']" /> -->
                 <!-- <Icon icon="ant-design:github-outlined" /> -->
                 Github
              </a>
              <!-- <button class="bg-blue-600 text-gray-200  p-2 rounded  hover:bg-blue-500 hover:text-gray-100">Sign up</button> -->
           </div>
        </div>
     </div>
  </nav>
